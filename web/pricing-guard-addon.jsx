import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{ShieldAlert,TrendingUp,Settings,RefreshCw}from'lucide-react';import'./pricing-guard.css';const A='/pricing-api/v1',tok=()=>localStorage.token,m=v=>Number(v||0).toLocaleString('ru-KZ')+' ₸';async function api(p,o={}){const r=await fetch(A+p,{...o,headers:{'Content-Type':'application/json',Authorization:`Bearer ${tok()}`}}),j=await r.json();if(!r.ok)throw Error(j.error?.message||'Ошибка');return j.data}function Guard({id}){const[d,setD]=useState(null),[err,setErr]=useState('');async function load(){try{setD(await api('/requests/'+id+'/check'));setErr('')}catch(e){setErr(e.message)}}useEffect(()=>{load();const f=()=>load();window.addEventListener('profi24:request-updated',f);return()=>window.removeEventListener('profi24:request-updated',f)},[id]);if(err)return <div className="pgErr">{err}</div>;if(!d)return null;return <section className={'pg '+d.level.toLowerCase()}><header><div><ShieldAlert/><b>Защита прибыли</b></div><button onClick={load}><RefreshCw size={15}/></button></header><div className="pgMain"><div><small>Цена клиенту</small><b>{m(d.price)}</b></div><div><small>Прогноз прибыли</small><b>{m(d.net_profit)}</b></div><div><small>Чистая маржа</small><b>{d.net_margin.toFixed(1)}%</b></div></div>{d.level!=='GOOD'&&<div className="pgAdvice"><TrendingUp/><span>{d.level==='DANGER'?'Цена ниже безопасного уровня.':'Маржа допустима, но ниже целевой.'}<b>Минимум {m(d.minimum_price)} · рекомендуем {m(d.recommended_price)}</b></span></div>}<div className="pgCosts"><span>Себестоимость <b>{m(d.direct_cost)}</b></span><span>Комиссии <b>{m(d.payroll_estimate)}</b></span><span>Накладные <b>{m(d.overhead_estimate)}</b></span></div>{!d.can_send&&<p className="pgBlock">⛔ Отправка сметы должна быть заблокирована политикой минимальной маржи.</p>}</section>}
function App(){const user=(()=>{try{return JSON.parse(localStorage.user||'null')}catch{return null}})(),[settings,setSettings]=useState(null);useEffect(()=>{if(user?.role!=='OWNER'||!window.Profi24UI)return;return window.Profi24UI.registerNav({id:'margin-settings',label:'Маржа',group:'management',onClick:async()=>setSettings(await api('/settings'))})},[user]);async function save(){await api('/settings',{method:'PUT',body:JSON.stringify(settings)});setSettings(null);window.dispatchEvent(new Event('profi24:request-updated'))}useEffect(()=>{const t=setInterval(()=>{const hero=document.querySelector('.o360Hero'),card=document.querySelector('.o360Card');if(!hero||!card||document.querySelector('.pgMount'))return;const selected=document.querySelector('.o360List button.on'),id=selected?.dataset?.id;if(!id)return;const h=document.createElement('div');h.className='pgMount';card.parentElement.insertBefore(h,card);createRoot(h).render(<Guard id={id}/>)},900);return()=>clearInterval(t)},[]);return settings&&<div className="pgModal"><div><h2>Политика рентабельности</h2><label>Минимальная чистая маржа, %<input type="number" value={settings.min_net_margin} onChange={e=>setSettings({...settings,min_net_margin:e.target.value})}/></label><label>Целевая чистая маржа, %<input type="number" value={settings.target_net_margin} onChange={e=>setSettings({...settings,target_net_margin:e.target.value})}/></label><label className="check"><input type="checkbox" checked={settings.block_below_min} onChange={e=>setSettings({...settings,block_below_min:e.target.checked})}/> Блокировать отправку убыточной сметы</label><button onClick={save}>Сохранить</button><button className="cancel" onClick={()=>setSettings(null)}>Отмена</button></div></div>}const host=document.createElement('div');document.body.appendChild(host);createRoot(host).render(<App/>);