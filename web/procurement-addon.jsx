import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{ShoppingCart,Plus,AlertTriangle,Bookmark,Truck,X,RefreshCw}from'lucide-react';import'./warehouse.css';
const P='/procurement-api/v1',W='/warehouse-api/v1',A='/api/v1';const token=()=>localStorage.token;async function c(base,path,opt={}){const r=await fetch(base+path,{...opt,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token()}`}});const j=await r.json();if(!r.ok)throw new Error(j.error?.message||'Ошибка');return j.data}const money=n=>new Intl.NumberFormat('ru-KZ').format(Number(n||0))+' ₸';
function App(){const[open,setOpen]=useState(false),[tab,setTab]=useState('alerts'),[sup,setSup]=useState([]),[orders,setOrders]=useState([]),[alerts,setAlerts]=useState([]),[res,setRes]=useState([]),[stock,setStock]=useState([]),[requests,setRequests]=useState([]),[modal,setModal]=useState(null),[err,setErr]=useState('');const user=(()=>{try{return JSON.parse(localStorage.user||'null')}catch{return null}})();async function load(){try{setErr('');const[s,o,al,r,st,rq]=await Promise.all([c(P,'/suppliers'),c(P,'/orders'),c(P,'/alerts'),c(P,'/reservations'),c(W,'/stock'),c(A,'/requests')]);setSup(s);setOrders(o);setAlerts(al);setRes(r);setStock(st);setRequests(rq)}catch(e){setErr(e.message)}}useEffect(()=>{if(open)load()},[open]);useEffect(()=>{if(!user||user.role==='ENGINEER'||!window.Profi24UI)return;return window.Profi24UI.registerNav({id:'procurement',label:'Закупки',group:'stock',badge:alerts.length||null,onClick:()=>setOpen(true)})},[user]);useEffect(()=>{window.Profi24UI?.updateNav('procurement',{badge:alerts.length||null})},[alerts.length]);if(!user||user.role==='ENGINEER'||!open)return null;return <div className="warehouseScreen"><div className="whTop"><div><h1>Закупки</h1><p>Поставщики, заказы, резервы и контроль минимальных остатков</p></div><div className="whTopActions"><button onClick={load}><RefreshCw size={18}/></button><button onClick={()=>setOpen(false)}><X/></button></div></div>{err&&<div className="whError">{err}</div>}<div className="whTabs"><button className={tab==='alerts'?'on':''} onClick={()=>setTab('alerts')}><AlertTriangle/>Нужно заказать <b>{alerts.length}</b></button><button className={tab==='orders'?'on':''} onClick={()=>setTab('orders')}><Truck/>Заказы поставщикам</button><button className={tab==='sup'?'on':''} onClick={()=>setTab('sup')}><ShoppingCart/>Поставщики</button><button className={tab==='res'?'on':''} onClick={()=>setTab('res')}><Bookmark/>Резервы</button></div>{tab==='alerts'&&<Box title="Минимальные остатки" action={<button className="whPrimary" onClick={()=>setModal('order')}><Plus/>Заказ поставщику</button>}>{alerts.length?alerts.map(x=><Row key={x.id} a={x.name} b={`Свободно ${x.available} · минимум ${x.min_quantity}`} c={x.supplier||'Поставщик не указан'} d={money(x.purchase_price)}/>):<div className="whEmpty">Все остатки выше минимального уровня</div>}</Box>}{tab==='sup'&&<Box title="Поставщики" action={<button className="whPrimary" onClick={()=>setModal('supplier')}><Plus/>Добавить</button>}>{sup.map(x=><Row key={x.id} a={x.name} b={x.contact_person||x.phone||'—'} c={x.bin||'БИН не указан'} d={x.email||''}/>)}</Box>}{tab==='orders'&&<Box title="Заказы поставщикам" action={<button className="whPrimary" onClick={()=>setModal('order')}><Plus/>Новый заказ</button>}>{orders.map(x=><Row key={x.id} a={x.number} b={x.supplier_name} c={`${x.positions} поз. · ${x.status}`} d={money(x.total)} action={x.status!=='RECEIVED'?<button onClick={async()=>{await c(P,`/orders/${x.id}/receive`,{method:'POST',body:'{}'});load()}}>Принять приход</button>:null}/>)}</Box>}{tab==='res'&&<Box title="Резервы под заказы" action={<button className="whPrimary" onClick={()=>setModal('reserve')}><Plus/>Зарезервировать</button>}>{res.map(x=><Row key={x.id} a={x.item_name} b={`${x.request_number} · ${x.customer_name}`} c={`Количество ${x.quantity}`} d="" action={<button onClick={async()=>{await c(P,`/reservations/${x.id}/release`,{method:'POST',body:'{}'});load()}}>Снять резерв</button>}/>)}</Box>}{modal&&<Modal type={modal} close={()=>setModal(null)} done={()=>{setModal(null);load()}} sup={sup} stock={stock} requests={requests}/>}</div>}
function Box({title,action,children}){return <div className="whTable"><div className="whTop"><h2>{title}</h2>{action}</div>{children}</div>}function Row({a,b,c,d,action}){return <div className="whRow"><div><b>{a}</b><small>{b}</small></div><span>{c}</span><b>{d}</b><span></span><div>{action}</div></div>}
function Modal({type,close,done,sup,stock,requests}){const[f,setF]=useState({name:'',phone:'',contact_person:'',bin:'',supplier_id:'',expected_at:'',comment:'',item_id:'',request_id:'',quantity:1}),[e,setE]=useState('');async function save(){try{if(type==='supplier')await c(P,'/suppliers',{method:'POST',body:JSON.stringify(f)});if(type==='order')await c(P,'/orders',{method:'POST',body:JSON.stringify(f)});if(type==='reserve')await c(P,'/reservations',{method:'POST',body:JSON.stringify(f)});done()}catch(x){setE(x.message)}}return <div className="whOverlay"><div className="whModal small"><div className="whModalHead"><h2>{type==='supplier'?'Новый поставщик':type==='order'?'Новый заказ поставщику':'Резерв запчасти'}</h2><button onClick={close}><X/></button></div>{e&&<div className="whError">{e}</div>}<div className="whForm">{type==='supplier'&&<><label>Название *<input value={f.name} onChange={x=>setF({...f,name:x.target.value})}/></label><label>Контактное лицо<input value={f.contact_person} onChange={x=>setF({...f,contact_person:x.target.value})}/></label><label>Телефон<input value={f.phone} onChange={x=>setF({...f,phone:x.target.value})}/></label><label>БИН<input value={f.bin} onChange={x=>setF({...f,bin:x.target.value})}/></label></>}{type==='order'&&<><label>Поставщик<select value={f.supplier_id} onChange={x=>setF({...f,supplier_id:+x.target.value})}><option value="">Выберите</option>{sup.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label>Ожидаемая дата<input type="date" value={f.expected_at} onChange={x=>setF({...f,expected_at:x.target.value})}/></label><label>Комментарий<textarea value={f.comment} onChange={x=>setF({...f,comment:x.target.value})}/></label><small>После создания позиции заказа можно добавлять через API; экран редактирования состава будет следующим улучшением.</small></>}{type==='reserve'&&<><label>Запчасть<select value={f.item_id} onChange={x=>setF({...f,item_id:+x.target.value})}><option value="">Выберите</option>{stock.map(s=><option key={s.id} value={s.id}>{s.name} · остаток {s.quantity}</option>)}</select></label><label>Заказ<select value={f.request_id} onChange={x=>setF({...f,request_id:+x.target.value})}><option value="">Выберите</option>{requests.filter(r=>!['CLOSED','CANCELLED'].includes(r.status)).map(r=><option key={r.id} value={r.id}>{r.number} · {r.customer_name}</option>)}</select></label><label>Количество<input type="number" value={f.quantity} onChange={x=>setF({...f,quantity:+x.target.value})}/></label></>}<div className="whButtons"><button onClick={close}>Отмена</button><button className="whPrimary" onClick={save}>Сохранить</button></div></div></div></div>}
const root=document.createElement('div');document.body.appendChild(root);createRoot(root).render(<App/>);