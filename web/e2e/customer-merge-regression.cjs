const{chromium}=require('playwright');
const fs=require('fs'),path=require('path');
const BASE=process.env.BASE_URL||'http://127.0.0.1:5173',EMAIL=process.env.E2E_EMAIL,PASSWORD=process.env.E2E_PASSWORD,artifacts=path.join(__dirname,'artifacts');fs.mkdirSync(artifacts,{recursive:true});
function fail(message){throw new Error(message)}
async function login(page){await page.goto(BASE,{waitUntil:'domcontentloaded'});await page.locator('input[autocomplete="username"]').fill(EMAIL);await page.locator('input[autocomplete="current-password"]').fill(PASSWORD);await page.getByRole('button',{name:'Войти'}).click();await page.waitForFunction(email=>{try{return JSON.parse(localStorage.user||'null')?.email===email}catch{return false}},EMAIL,{timeout:10000});await page.locator('aside').waitFor({state:'visible',timeout:10000})}
async function api(page,url,{method='GET',body}={}){return page.evaluate(async({url,method,body})=>{const r=await fetch(url,{method,headers:{Authorization:`Bearer ${localStorage.token||''}`,...(body===undefined?{}:{'Content-Type':'application/json'})},body:body===undefined?undefined:JSON.stringify(body)}),text=await r.text();let j={};try{j=JSON.parse(text)}catch{}return{status:r.status,data:j.data,error:j.error,text}}, {url,method,body})}
(async()=>{const browser=await chromium.launch({headless:true}),context=await browser.newContext(),page=await context.newPage();try{
 await login(page);const suffix=Date.now().toString(36).toUpperCase(),phone=`+7709${String(Date.now()).slice(-7)}`;
 let r=await api(page,'/api/v1/customers',{method:'POST',body:{name:`E2E Основной ${suffix}`,phone,email:`main-${suffix}@test.invalid`}});if(r.status!==201)fail(`target ${r.status} ${r.text}`);const target=r.data;
 r=await api(page,'/api/v1/customers',{method:'POST',body:{name:`E2E Дубликат ${suffix}`,phone,address:'Костанай, адрес дубликата',force:true}});if(r.status!==201)fail(`source ${r.status} ${r.text}`);const source=r.data;
 r=await api(page,'/api/v1/equipment',{method:'POST',body:{customer_id:source.id,category:'Холодильник',brand:'E2E',model:`MERGE-${suffix}`}});if(r.status!==201)fail(`equipment ${r.status}`);const equipment=r.data;
 r=await api(page,'/api/v1/requests',{method:'POST',body:{customer_id:source.id,equipment_id:equipment.id,complaint:'Browser customer merge acceptance',source:'OTHER'}});if(r.status!==201)fail(`order ${r.status}`);const order=r.data;
 await page.reload({waitUntil:'domcontentloaded'});await page.locator('aside').getByRole('button',{name:/^Клиенты/}).click();await page.getByRole('heading',{name:'Клиенты'}).waitFor({state:'visible',timeout:10000});
 const row=page.locator('main .simple').filter({hasText:`E2E Дубликат ${suffix}`});await row.waitFor({state:'visible',timeout:10000});const button=row.locator('.dirMerge');await button.waitFor({state:'visible',timeout:10000});await button.dispatchEvent('pointerdown');
 const modal=page.locator('.dirModal').filter({hasText:'Объединить дубликаты клиентов'});await modal.waitFor({state:'visible',timeout:10000});await modal.locator('[data-target]').selectOption(String(target.id));await modal.getByText('Телефоны совпадают',{exact:true}).waitFor({state:'visible',timeout:10000});await modal.locator('[data-reason]').fill('Повторная карточка в browser acceptance');
 page.once('dialog',dialog=>dialog.accept());await modal.getByRole('button',{name:'Объединить',exact:true}).click();await modal.waitFor({state:'detached',timeout:10000});
 r=await api(page,`/directory-api/v1/customers/${source.id}/merge-preview?target_id=${target.id}`);if(r.status!==409||r.error?.code!=='CUSTOMER_INACTIVE')fail(`source remains active ${r.status} ${r.text}`);
 r=await api(page,`/api/v1/requests/${order.id}`);if(r.status!==200||Number(r.data?.customer_id)!==Number(target.id))fail(`order history not moved ${r.status} ${r.text}`);
 await page.locator('main .simple').filter({hasText:`E2E Дубликат ${suffix}`}).waitFor({state:'detached',timeout:10000});
 console.log(`customer_merge_regression=ok source=${source.id} target=${target.id} order=${order.number}`);
}catch(error){try{await page.screenshot({path:path.join(artifacts,'customer-merge-failure.png'),fullPage:true})}catch{}fs.writeFileSync(path.join(artifacts,'customer-merge-error.txt'),String(error.stack||error));console.error(error);process.exitCode=1}finally{await context.close().catch(()=>{});await browser.close()}})();
