import React,{useEffect,useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{X,RefreshCw,UserRound,AlertTriangle,ChevronRight,ChevronDown,ShieldCheck,ExternalLink,Target,TrendingUp,TrendingDown,Minus,Activity,Clock3,BarChart3,Lightbulb}from'lucide-react';
import'./discipline.css';
const H='/discipline-api/discipline',token=()=>localStorage.token;
async function get(url){const r=await fetch(url,{headers:{Authorization:`Bearer ${token()}`}}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error?.message||'Ошибка');return j.data}
const hist=p=>get(H+p),dt=v=>v?new Date(v).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}):'—';
const sev=w=>Number(w)>=3?'critical':Number(w)===2?'warning':'info',sevName=s=>s==='critical'?'Критично':s==='warning'?'Внимание':'Низкий';
function groupCases(items){const m=new Map();for(const x of items){const key=x.request_id?`r:${x.request_id}`:`i:${x.id}`;if(!m.has(key))m.set(key,{key,number:x.number,client:x.customer_name,signals:[],weight:0,opened_at:x.opened_at});const c=m.get(key);c.signals.push(x);c.weight=Math.max(c.weight,Number(x.weight||0));if(new Date(x.opened_at)<new Date(c.opened_at))c.opened_at=x.opened_at}return[...m.values()].map(c=>({...c,primary:[...c.signals].sort((a,b)=>Number(b.weight)-Number(a.weight))[0],severity:sev(c.weight)})).sort((a,b)=>b.weight-a.weight||new Date(a.opened_at)-new Date(b.opened_at))}
function openOrder(number,close){if(!number)return;close();setTimeout(()=>{const nav=[...document.querySelectorAll('.shell>aside nav button')].find(b=>(b.dataset.coreLabel||b.textContent||'').trim().startsWith('Заказы')&&!b.hidden);nav?.click();let n=0,i=setInterval(()=>{n++;const row=[...document.querySelectorAll('.trow')].find(r=>(r.textContent||'').includes(number));if(row){clearInterval(i);row.click()}else if(n>30)clearInterval(i)},100)},50)}
function advice(row){const all=row.all,counts={};all.forEach(x=>counts[x.type]=(counts[x.type]||0)+1);const top=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0];const names={SLA:'соблюдение SLA',ACCEPT:'скорость принятия заявок',VISIT:'своевременный выезд',APPROVAL:'согласования',PART:'работа с запчастями',REPAIR:'срок ремонта',CONTROL:'контрольные сроки'};if(row.rating<50)return`Критическая зона. Разобрать активные кейсы сегодня${top?`; основной повторяющийся риск — ${names[top[0]]||top[0]}`:''}.`;if(Number(row.u.open_cases)>0)return`Есть активные проблемные заказы. Приоритет — закрыть ${row.u.open_cases} кейс. и проверить ${top?names[top[0]]||top[0]:'сроки исполнения'}.`;if(row.rating<90)return`Активных проблем нет, но история снижает рейтинг. Контролировать ${top?names[top[0]]||top[0]:'повторные нарушения'} до восстановления рейтинга.`;return'Стабильная дисциплина. Вмешательство руководителя не требуется.'}
function App(){const[open,setOpen]=useState(false),[summary,setSummary]=useState(null),[incidents,setIncidents]=useState([]),[days,setDays]=useState(7),[err,setErr]=useState(''),[selected,setSelected]=useState(null);const me=(()=>{try{return JSON.parse(localStorage.user||'null')}catch{return null}})();async function load(d=days){try{setErr('');const[a,b]=await Promise.all([hist(`/summary?days=${d}`),hist(`/incidents?days=${d}`)]);setSummary(a||null);setIncidents(b||[])}catch(x){setErr(x.message)}}useEffect(()=>{if(open){load(days);const i=setInterval(()=>load(days),30000);return()=>clearInterval(i)}},[open,days]);useEffect(()=>{if(!['OWNER','MANAGER'].includes(me?.role)||!window.Profi24UI)return;return window.Profi24UI.registerNav({id:'discipline',label:'Дисциплина',group:'team',roles:['OWNER','MANAGER'],onClick:()=>setOpen(true)})},[me]);const rows=useMemo(()=>{const s=summary?.users||[];return s.map(u=>{const all=incidents.filter(i=>Number(i.user_id)===Number(u.id)),current=groupCases(all.filter(i=>!i.resolved_at)),period=all.filter(i=>new Date(i.opened_at)>=new Date(Date.now()-days*864e5)),rating=Number(u.rating??100),level=rating<50?'red':rating<80?'yellow':'green';return{u,current,period,all,rating,level}}).sort((a,b)=>a.rating-b.rating||Number(b.u.open_cases)-Number(a.u.open_cases))},[summary,incidents,days]);if(!open)return null;const totals={red:rows.filter(x=>x.level==='red').length,yellow:rows.filter(x=>x.level==='yellow').length,green:rows.filter(x=>x.level==='green').length,cases:rows.reduce((s,x)=>s+Number(x.u.period_cases||0),0)},priority=rows.filter(x=>Number(x.u.open_cases)>0||x.level!=='green').slice(0,3);return <div className="disciplineScreen"><div className="disciplineTop"><div><h1>Исполнительская дисциплина</h1><p>Контроль проблемных заказов и управленческий профиль сотрудников</p></div><div className="disciplineActions"><div className="discPeriods"><button className={days===1?'on':''} onClick={()=>setDays(1)}>Сегодня</button><button className={days===7?'on':''} onClick={()=>setDays(7)}>7 дней</button><button className={days===30?'on':''} onClick={()=>setDays(30)}>30 дней</button></div><button title="Обновить" onClick={()=>load(days)}><RefreshCw size={18}/></button><button title="Закрыть" onClick={()=>setOpen(false)}><X size={20}/></button></div></div>{err&&<div className="disciplineError">{err}</div>}<div className="disciplineMetrics"><M t="Критические сейчас" v={totals.red} cls="red"/><M t="Требуют внимания" v={totals.yellow} cls="yellow"/><M t="Без нарушений" v={totals.green} cls="green"/><M t={`Проблемных кейсов за ${days===1?'сегодня':days+' дней'}`} v={totals.cases}/></div>{priority.length>0&&<section className="discPriority"><div className="discPriorityTitle"><Target size={18}/><div><b>Приоритет руководителя</b><small>Сначала сотрудники с активными проблемными заказами и низким рейтингом</small></div></div>{priority.map((r,i)=><button key={r.u.id} onClick={()=>setSelected(r.u.id)}><strong>{i+1}</strong><span><b>{r.u.name}</b><small>{r.u.open_cases} кейс. сейчас · {r.u.open_incidents} сигнал. · рейтинг {r.rating}/100</small></span><em>{r.level==='red'?'Разобрать сегодня':'Проверить'}</em><ChevronRight size={17}/></button>)}</section>}<section className="disciplineTable"><div className="disciplineHead hist"><span>Сотрудник</span><span>Активные</span><span>Кейсы</span><span>За период</span><span>Принятие</span><span>Рейтинг</span><span></span></div>{rows.map(r=><React.Fragment key={r.u.id}><button className={'disciplineRow hist '+r.level} onClick={()=>setSelected(selected===r.u.id?null:r.u.id)}><span className="discPerson"><UserRound size={18}/><b>{r.u.name}</b><small>{r.u.role==='ENGINEER'?'Инженер':'Менеджер'}</small></span><b>{r.u.active}</b><span className="discBad"><b>{r.u.open_cases}</b><small>{r.u.open_incidents} сигнал.</small></span><span className="discPeriodCount"><b>{r.u.period_cases}</b><Trend value={r.u.trend}/></span><span className="discAccept"><b>{r.u.avg_accept_minutes==null?'—':r.u.avg_accept_minutes+' мин'}</b><small>{r.u.accept_within_target_pct==null?'нет данных':r.u.accept_within_target_pct+'% ≤ 2ч'}</small></span><span className={'discLevel '+r.level}>{r.rating}/100</span>{selected===r.u.id?<ChevronDown size={18}/>:<ChevronRight size={18}/>}</button>{selected===r.u.id&&<Profile row={r} close={()=>setOpen(false)} days={days}/>}</React.Fragment>)}</section><div className="discFoot">Discipline v3 · рейтинг, повторяемость нарушений и управленческий вывод по сотруднику.</div></div>}
function Trend({value}){const n=Number(value||0);if(n>0)return <em className="trend worse"><TrendingUp size={13}/>+{n}</em>;if(n<0)return <em className="trend better"><TrendingDown size={13}/>{n}</em>;return <em className="trend same"><Minus size={13}/>0</em>}
function Profile({row,close,days}){const types={};row.period.forEach(x=>types[x.label]=(types[x.label]||0)+1);const repeated=Object.entries(types).sort((a,b)=>b[1]-a[1]).slice(0,3);return <div className="disciplineDetail"><div className="discProfileHead"><div><span className="discAvatar"><UserRound size={22}/></span><div><b>{row.u.name}</b><small>{row.u.role==='ENGINEER'?'Инженер':'Менеджер'} · управленческий профиль</small></div></div><span className={'discBigScore '+row.level}>{row.rating}<small>/100</small></span></div><div className="discProfileMetrics"><PM icon={<Activity size={16}/>} t="Активные кейсы" v={row.u.open_cases||0} s={`${row.u.open_incidents||0} сигналов`}/><PM icon={<BarChart3 size={16}/>} t={`За ${days===1?'сегодня':days+' дней'}`} v={row.u.period_cases||0} s={`динамика ${Number(row.u.trend)>0?'+':''}${row.u.trend||0}`}/><PM icon={<Clock3 size={16}/>} t="Принятие заявки" v={row.u.avg_accept_minutes==null?'—':row.u.avg_accept_minutes+' мин'} s={row.u.accept_within_target_pct==null?'нет данных':row.u.accept_within_target_pct+'% в нормативе'}/><PM icon={<ShieldCheck size={16}/>} t="Штраф рейтинга" v={'−'+((row.u.rating_current_penalty||0)+(row.u.rating_history_penalty||0))} s={`сейчас −${row.u.rating_current_penalty||0} · история −${row.u.rating_history_penalty||0}`}/></div><div className="discAdvice"><Lightbulb size={18}/><div><b>Вывод для руководителя</b><span>{advice(row)}</span></div></div>{repeated.length>0&&<div className="discRepeat"><b>Повторяемость за период</b><div>{repeated.map(([name,n])=><span key={name}>{name}<strong>{n}</strong></span>)}</div></div>}<div className="discDetailTitle">Активные проблемные кейсы</div>{row.current.length?row.current.map(c=><button className={'discCase '+(c.number?'clickable':'')} key={c.key} onClick={()=>c.number&&openOrder(c.number,close)}><AlertTriangle size={17}/><div><b>{c.primary?.label||'Нарушение'} <i className={'discSeverity '+c.severity}>{sevName(c.severity)}</i></b><span>{c.number||'Поручение'} · {c.client||'—'}</span><small>{c.signals.map(x=>x.label).join(' · ')}</small></div><div className="discCaseMeta"><b>вес {c.weight}</b><small>{c.signals.length} сигнал.</small></div>{c.number&&<ExternalLink size={15}/>}</button>):<div className="discClean"><ShieldCheck size={18}/>Активных проблемных кейсов нет.</div>}<div className="discDetailTitle history">История событий за {days===1?'сегодня':days+' дней'}</div>{row.period.length?row.period.map(x=><button className={'discIssue '+(x.number?'clickable':'')} key={'h'+x.id} onClick={()=>x.number&&openOrder(x.number,close)}><AlertTriangle size={16}/><div><b>{x.label}<i className={'discSeverity '+(x.severity||sev(x.weight))}>{sevName(x.severity||sev(x.weight))}</i></b><span>{x.number||'Поручение'} · {x.customer_name||'—'} · {x.resolved_at?'устранено':'активно'}</span></div><small>{dt(x.opened_at)}</small>{x.number&&<ExternalLink size={15}/>}</button>):<div className="discClean"><ShieldCheck size={18}/>За выбранный период новых событий нет.</div>}</div>}
function PM({icon,t,v,s}){return <div className="discPM"><span>{icon}</span><div><small>{t}</small><b>{v}</b><em>{s}</em></div></div>}
function M({t,v,cls=''}){return <div className={'disciplineMetric '+cls}><small>{t}</small><b>{v}</b></div>}
const root=document.createElement('div');document.body.appendChild(root);createRoot(root).render(<App/>);
