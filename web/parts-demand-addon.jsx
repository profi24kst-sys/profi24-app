import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{PackageSearch,RefreshCw,ShoppingCart,Clock3}from'lucide-react';import'./parts-demand.css';const A='/parts-api/v1',tok=()=>localStorage.token;async function api(p,o={}){const r=await fetch(A+p,{...o,headers:{'Content-Type':'application/json',Authorization:`Bearer ${tok()}`}}),j=await r.json();if(!r.ok)throw Error(j.error?.message||'Ошибка');return j.data}function App(){const user=(()=>{try{return JSON.parse(localStorage.user||'null')}catch{return null}})(),[open,setOpen]=useState(false),[rows,setRows]=useState([]),[msg,setMsg]=useState('');async function load(){try{setRows(await api('/demands'));setMsg('')}catch(e){setMsg(e.message)}}async function sync(){const x=await api('/sync',{method:'POST'});setMsg(x.resumed?`Возвращено в ремонт: ${x.resumed}`:'Новых готовых заявок нет');load()}useEffect(()=>{if(open){load();const t=setInterval(load,30000);return()=>clearInterval(t)}},[open]);useEffect(()=>{if(!['OWNER','MANAGER'].includes(user?.role))return;const nav=document.querySelector('aside nav');if(!nav)return;const h=document.createElement('div');nav.appendChild(h);const root=createRoot(h);root.render(<button onClick={()=>setOpen(true)}><PackageSearch size={18}/><span>Ожидание запчастей</span></button>);return()=>{root.unmount();h.remove()}},[user]);if(!open)return null;return <div className="pd"><header><div><h1>Ожидание запчастей</h1><p>Автозакупка и возврат заявок в ремонт</p></div><div><button onClick={sync}><RefreshCw/>Проверить склад</button><button onClick={()=>setOpen(false)}>×</button></div></header>{msg&&<div className="pdMsg">{msg}</div>}<section>{rows.length===0?<div className="pdEmpty">Нет заявок, ожидающих закупку.</div>:rows.map(x=><article key={x.id}><div className="pdOrder"><small>Заявка</small><b>{x.request_number}</b><span>{x.customer_name}</span></div><div className="pdPart"><PackageSearch/><span><b>{x.item_name}</b><small>{x.oem_code||x.sku||'без кода'} · нужно {x.qty} · на складе {x.stock_qty}</small></span></div><div className="pdSupplier"><small>Поставщик</small><b>{x.supplier||'не назначен'}</b></div><div className="pdPo">{x.purchase_order_number?<><ShoppingCart/><span><b>{x.purchase_order_number}</b><small>{x.purchase_order_status}</small></span></>:<><Clock3/><span><b>Требуется закупка</b><small>Назначьте поставщика</small></span></>}</div></article>)}</section></div>}const host=document.createElement('div');document.body.appendChild(host);createRoot(host).render(<App/>);