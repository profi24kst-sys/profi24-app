import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{Navigation,MapPin,Send,Wrench,ShieldCheck,Wallet,CheckCircle2,ArrowRight,Clock3}from'lucide-react';import'./workflow.css';const W='/workflow-api/v1',tok=()=>localStorage.token;const names={NEW:'Принят',ASSIGNED:'Назначен',ACCEPTED:'Принят инженером',ON_ROUTE:'В пути',DIAGNOSTICS:'Диагностика',APPROVAL_REQUIRED:'Согласование',WAITING_PART:'Ожидание детали',REPAIR:'Ремонт',TESTING:'Проверка',PAYMENT_REQUIRED:'Оплата',CLOSED:'Закрыт'};const icons={DEPART:Navigation,ARRIVE:MapPin,SEND_APPROVAL:Send,START_REPAIR:Wrench,START_TEST:ShieldCheck,REQUEST_PAYMENT:Wallet,CLOSE:CheckCircle2};async function api(p,o={}){const r=await fetch(W+p,{...o,headers:{'Content-Type':'application/json',Authorization:`Bearer ${tok()}`}}),j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error?.message||'Ошибка');return j.data}async function core(id){const r=await fetch('/api/v1/requests/'+id,{headers:{Authorization:`Bearer ${tok()}`}}),j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error?.message||'Ошибка');return j.data}const m=v=>Number(v||0).toLocaleString('ru-KZ')+' ₸';function Flow({id}){const[x,setX]=useState(null),[order,setOrder]=useState(null),[busy,setBusy]=useState(false),[err,setErr]=useState('');async function load(){if(id)try{const[a,b]=await Promise.all([api('/requests/'+id+'/workflow'),core(id)]);setX(a);setOrder(b);setErr('')}catch(e){setErr(e.message)}}useEffect(()=>{load();const h=e=>{if(!e.detail?.id||String(e.detail.id)===String(id))load()};window.addEventListener('profi24:request-updated',h);return()=>window.removeEventListener('profi24:request-updated',h)},[id]);if(!x)return null;const I=icons[x.next?.event]||ArrowRight,balance=Math.max(0,Number(order?.total||0)-Number(order?.paid||0)),closeBlocked=x.next?.event==='CLOSE'&&balance>0;async function go(){if(closeBlocked)return;try{setBusy(true);setErr('');await api('/requests/'+id+'/workflow',{method:'POST',body:JSON.stringify({event:x.next.event})});await load();window.dispatchEvent(new CustomEvent('profi24:request-updated',{detail:{id}}))}catch(e){setErr(e.message)}finally{setBusy(false)}}return <section className="wf"><header><div><Clock3 size={17}/><b>Жизненный цикл заказа</b></div><span>{names[x.status]||x.status}</span></header><div className="wfTrack">{['NEW','ASSIGNED','ACCEPTED','ON_ROUTE','DIAGNOSTICS','APPROVAL_REQUIRED','REPAIR','TESTING','PAYMENT_REQUIRED','CLOSED'].map((s,i)=>{const order=['NEW','ASSIGNED','ACCEPTED','ON_ROUTE','DIAGNOSTICS','APPROVAL_REQUIRED','REPAIR','TESTING','PAYMENT_REQUIRED','CLOSED'],cur=order.indexOf(x.status);return <div className={i<cur?'done':i===cur?'now':''} key={s}><i>{i<cur?'✓':i+1}</i><small>{names[s]}</small></div>})}</div>{err&&<p className="wfErr">{err}</p>}{closeBlocked&&<div className="wfBlock"><b>Заказ нельзя закрыть</b><span>Необходимо принять оплату {m(balance)}. После полной оплаты кнопка закрытия станет доступна автоматически.</span></div>}{x.next&&!closeBlocked&&<button disabled={busy} onClick={go}><I size={18}/>{busy?'Сохраняю…':x.next.label}<ArrowRight size={17}/></button>}<div className="wfTimes">{x.events.slice(-5).reverse().map(e=><span key={e.id}><b>{names[e.stage]||e.stage}</b>{new Date(e.created_at).toLocaleString('ru-RU')} · {e.user_name||'Система'}</span>)}</div></section>}
function mount(){const cards=[...document.querySelectorAll('.o360Card')],target=cards[0],hero=document.querySelector('.o360Hero');if(!target||!hero||document.querySelector('.wfMount'))return;const selected=[...document.querySelectorAll('.o360List button')].find(b=>b.classList.contains('on')),buttons=[...document.querySelectorAll('.o360List button')],idx=buttons.indexOf(selected),all=JSON.parse(sessionStorage.getItem('wf-orders')||'[]'),id=selected?.dataset?.id||all[idx]?.id;if(!id)return;const d=document.createElement('div');d.className='wfMount';target.parentElement.insertBefore(d,target);createRoot(d).render(<Flow id={id}/>)}
let annotating=false,lastSync=0;async function annotate(force=false){if(!document.querySelector('.o360')||annotating)return;if(!force&&Date.now()-lastSync<15000){mount();return}annotating=true;try{const r=await fetch('/api/v1/requests',{headers:{Authorization:`Bearer ${tok()}`}}),j=await r.json();if(!r.ok)return;const orders=j.data||[];lastSync=Date.now();sessionStorage.setItem('wf-orders',JSON.stringify(orders.map(x=>({id:x.id,number:x.number}))));document.querySelectorAll('.o360List button').forEach((b,i)=>{if(orders[i])b.dataset.id=orders[i].id});mount()}catch{}finally{annotating=false}}
let navTimer;new MutationObserver(()=>{clearTimeout(navTimer);navTimer=setTimeout(()=>{if(document.querySelector('.o360'))annotate(false)},400)}).observe(document.body,{childList:true,subtree:true});window.addEventListener('profi24:request-updated',()=>setTimeout(()=>annotate(true),150));setInterval(()=>{if(document.querySelector('.o360'))annotate(false)},15000);setTimeout(()=>annotate(true),500);