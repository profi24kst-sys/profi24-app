import Fastify from'fastify';import cors from'@fastify/cors';import helmet from'@fastify/helmet';import jwt from'@fastify/jwt';import pg from'pg';
const app=Fastify({logger:true});await app.register(cors,{origin:true,credentials:true});await app.register(helmet,{contentSecurityPolicy:false});await app.register(jwt,{secret:process.env.JWT_SECRET||'dev-secret-change-me'});
const pool=new pg.Pool({connectionString:process.env.DATABASE_URL}),q=(s,p=[])=>pool.query(s,p),n=v=>Number(v||0),fail=(r,c,m,s=422)=>r.code(s).send({data:null,error:{code:c,message:m}});
const auth=async(req,r)=>{try{await req.jwtVerify()}catch{return fail(r,'UNAUTHORIZED','Требуется авторизация',401)}},staff=async(req,r)=>{await auth(req,r);if(r.sent)return;if(!['OWNER','MANAGER'].includes(req.user.role))return fail(r,'FORBIDDEN','Недостаточно прав',403)};
for(const s of[`CREATE TABLE IF NOT EXISTS part_demands(id BIGSERIAL PRIMARY KEY,request_id INT NOT NULL REFERENCES requests(id) ON DELETE CASCADE,item_id INT NOT NULL REFERENCES warehouse_items(id),qty NUMERIC(14,3) NOT NULL,status TEXT NOT NULL DEFAULT 'NEED_PURCHASE',purchase_order_id INT REFERENCES purchase_orders(id),created_by INT REFERENCES users(id),created_at TIMESTAMPTZ DEFAULT now(),resolved_at TIMESTAMPTZ,UNIQUE(request_id,item_id))`,`CREATE INDEX IF NOT EXISTS idx_part_demands_status ON part_demands(status,created_at)`,`CREATE TABLE IF NOT EXISTS special_part_orders(id BIGSERIAL PRIMARY KEY,request_id INT NOT NULL REFERENCES requests(id) ON DELETE CASCADE,part_id INT REFERENCES parts(id) ON DELETE SET NULL,name TEXT NOT NULL,oem_code TEXT,qty NUMERIC(14,3) NOT NULL DEFAULT 1,sale_price NUMERIC(14,2) NOT NULL DEFAULT 0,purchase_price NUMERIC(14,2) NOT NULL DEFAULT 0,supplier TEXT,expected_at DATE,status TEXT NOT NULL DEFAULT 'NEED_PURCHASE',responsible_id INT REFERENCES users(id),warehouse_item_id INT REFERENCES warehouse_items(id),comment TEXT,created_by INT REFERENCES users(id),created_at TIMESTAMPTZ DEFAULT now(),updated_at TIMESTAMPTZ DEFAULT now(),received_at TIMESTAMPTZ,installed_at TIMESTAMPTZ)`,`CREATE INDEX IF NOT EXISTS idx_special_part_orders_status ON special_part_orders(status,expected_at,created_at)`])await q(s);
async function required(requestId,c=pool){return(await c.query(`SELECT ref_id item_id,sum(qty)::numeric qty FROM request_quote_lines WHERE request_id=$1 AND line_type='PART' GROUP BY ref_id`,[requestId])).rows}
async function availability(itemId,requestId,c=pool){return(await c.query(`SELECT w.*,w.quantity-COALESCE((SELECT sum(sr.quantity) FROM stock_reservations sr WHERE sr.item_id=w.id AND sr.status='ACTIVE' AND sr.request_id<>$2),0) available FROM warehouse_items w WHERE w.id=$1`,[itemId,requestId])).rows[0]}
async function setReservation(c,requestId,itemId,qty,userId){const old=(await c.query(`SELECT * FROM stock_reservations WHERE request_id=$1 AND item_id=$2 AND status='ACTIVE' ORDER BY id DESC LIMIT 1`,[requestId,itemId])).rows[0];if(old)return(await c.query(`UPDATE stock_reservations SET quantity=$1,created_by=COALESCE($2,created_by) WHERE id=$3 RETURNING *`,[qty,userId||null,old.id])).rows[0];return(await c.query(`INSERT INTO stock_reservations(item_id,request_id,quantity,status,created_by) VALUES($1,$2,$3,'ACTIVE',$4) RETURNING *`,[itemId,requestId,qty,userId||null])).rows[0]}
async function ensurePurchaseOrder(c,demand,userId){const item=(await c.query('SELECT * FROM warehouse_items WHERE id=$1',[demand.item_id])).rows[0];if(!item)return null;let supplier=null;if(item.supplier)supplier=(await c.query('SELECT * FROM suppliers WHERE active=true AND lower(name)=lower($1) LIMIT 1',[item.supplier])).rows[0];if(!supplier)return null;let po=(await c.query(`SELECT po.* FROM purchase_orders po WHERE po.supplier_id=$1 AND po.status IN('DRAFT','ORDERED') AND po.comment LIKE '%AUTO%' ORDER BY po.id DESC LIMIT 1`,[supplier.id])).rows[0];if(!po){const number=`PO-${new Date().getFullYear()}-${String(Date.now()).slice(-6)}`;po=(await c.query(`INSERT INTO purchase_orders(number,supplier_id,status,comment,created_by) VALUES($1,$2,'DRAFT','AUTO: сформирован по дефициту заявок',$3) RETURNING *`,[number,supplier.id,userId])).rows[0]}let line=(await c.query('SELECT * FROM purchase_order_items WHERE purchase_order_id=$1 AND item_id=$2',[po.id,item.id])).rows[0];if(line)await c.query('UPDATE purchase_order_items SET qty=qty+$1 WHERE id=$2',[demand.qty,line.id]);else await c.query('INSERT INTO purchase_order_items(purchase_order_id,item_id,name,qty,unit_cost) VALUES($1,$2,$3,$4,$5)',[po.id,item.id,item.name,demand.qty,item.purchase_price]);await c.query("UPDATE purchase_orders SET status='ORDERED',updated_at=now() WHERE id=$1",[po.id]);await c.query("UPDATE part_demands SET purchase_order_id=$1,status='ORDERED' WHERE id=$2",[po.id,demand.id]);return po}
async function prepare(requestId,userId){const c=await pool.connect();try{await c.query('BEGIN');const parts=await required(requestId,c);let reserved=0,shortages=[];for(const p of parts){const item=await availability(p.item_id,requestId,c);if(!item){shortages.push({item_id:p.item_id,qty:n(p.qty),missing:n(p.qty),name:`ID ${p.item_id}`});continue}const available=n(item.available),need=n(p.qty);if(available>=need){await setReservation(c,requestId,p.item_id,need,userId);await c.query("UPDATE part_demands SET status='RESOLVED',resolved_at=now() WHERE request_id=$1 AND item_id=$2 AND status<>'RESOLVED'",[requestId,p.item_id]);reserved++}else{const missing=Math.max(0,need-available);if(available>0)await setReservation(c,requestId,p.item_id,available,userId);const d=(await c.query(`INSERT INTO part_demands(request_id,item_id,qty,created_by) VALUES($1,$2,$3,$4) ON CONFLICT(request_id,item_id) DO UPDATE SET qty=$3,status=CASE WHEN part_demands.status='RESOLVED' THEN 'NEED_PURCHASE' ELSE part_demands.status END,resolved_at=NULL RETURNING *`,[requestId,p.item_id,missing,userId])).rows[0];const po=await ensurePurchaseOrder(c,d,userId);shortages.push({item_id:p.item_id,name:item.name,qty:need,available,missing,purchase_order_id:po?.id||null,purchase_order_number:po?.number||null})}}if(shortages.length){await c.query("UPDATE requests SET status='WAITING_PART',updated_at=now() WHERE id=$1",[requestId]);await c.query('INSERT INTO request_history(request_id,user_id,action,details) VALUES($1,$2,$3,$4)',[requestId,userId,'WAITING_PARTS',{shortages}])}else{await c.query("UPDATE requests SET status='REPAIR',updated_at=now() WHERE id=$1",[requestId]);await c.query("UPDATE request_quote_lines SET status='APPROVED' WHERE request_id=$1",[requestId])}await c.query('COMMIT');return{status:shortages.length?'WAITING_PART':'REPAIR',reserved,shortages}}catch(e){await c.query('ROLLBACK');throw e}finally{c.release()}}
async function resumeReady(){const reqs=(await q("SELECT DISTINCT request_id FROM part_demands WHERE status IN('NEED_PURCHASE','ORDERED')")).rows;let resumed=0;for(const x of reqs){const parts=await required(x.request_id);let ready=true;for(const p of parts){const item=await availability(p.item_id,x.request_id);if(!item||n(item.available)<n(p.qty)){ready=false;break}}if(!ready)continue;const c=await pool.connect();try{await c.query('BEGIN');for(const p of parts)await setReservation(c,x.request_id,p.item_id,p.qty,null);await c.query("UPDATE part_demands SET status='RESOLVED',resolved_at=now() WHERE request_id=$1 AND status<>'RESOLVED'",[x.request_id]);await c.query("UPDATE request_quote_lines SET status='APPROVED' WHERE request_id=$1",[x.request_id]);await c.query("UPDATE requests SET status='REPAIR',updated_at=now() WHERE id=$1 AND status='WAITING_PART'",[x.request_id]);await c.query('INSERT INTO request_history(request_id,action,details) VALUES($1,$2,$3)',[x.request_id,'PARTS_RECEIVED_AUTO_RESUME',{auto:true}]);await c.query('COMMIT');resumed++}catch(e){await c.query('ROLLBACK');app.log.error(e)}finally{c.release()}}return resumed}
const allowed=new Set(['NEED_PURCHASE','SEARCHING','PRICE_FOUND','APPROVED','ORDERED','PAID','IN_TRANSIT','RECEIVED','ISSUED','INSTALLED','CANCELLED']);
async function specialList(where='',params=[]){return(await q(`SELECT s.*,r.number request_number,r.status request_status,c.name customer_name,e.category,e.brand,e.model,e.serial_number,u.name created_by_name,resp.name responsible_name,CASE WHEN s.expected_at<CURRENT_DATE AND s.status NOT IN('RECEIVED','ISSUED','INSTALLED','CANCELLED') THEN true ELSE false END overdue FROM special_part_orders s JOIN requests r ON r.id=s.request_id JOIN customers c ON c.id=r.customer_id LEFT JOIN equipment e ON e.id=r.equipment_id LEFT JOIN users u ON u.id=s.created_by LEFT JOIN users resp ON resp.id=s.responsible_id WHERE r.deleted_at IS NULL ${where} ORDER BY overdue DESC,s.expected_at NULLS LAST,s.created_at DESC`,params)).rows}
app.get('/health',async()=>{await q('SELECT 1');return{ok:true,service:'profi24-parts-orchestrator',version:'2.0.0'}});
app.post('/api/v1/requests/:id/prepare',{preHandler:auth},async(req,r)=>{if(req.user.role==='ENGINEER'){const z=(await q('SELECT engineer_id FROM requests WHERE id=$1',[req.params.id])).rows[0];if(!z||z.engineer_id!==req.user.id)return fail(r,'FORBIDDEN','Заявка назначена другому инженеру',403)}return{data:await prepare(n(req.params.id),req.user.id)}});
app.get('/api/v1/demands',{preHandler:staff},async()=>({data:(await q(`SELECT d.*,r.number request_number,c.name customer_name,w.name item_name,w.sku,w.oem_code,w.quantity stock_qty,w.supplier,po.number purchase_order_number,po.status purchase_order_status FROM part_demands d JOIN requests r ON r.id=d.request_id JOIN customers c ON c.id=r.customer_id JOIN warehouse_items w ON w.id=d.item_id LEFT JOIN purchase_orders po ON po.id=d.purchase_order_id WHERE d.status<>'RESOLVED' ORDER BY d.created_at DESC`)).rows}));
app.post('/api/v1/sync',{preHandler:staff},async()=>({data:{resumed:await resumeReady()}}));
app.get('/api/v1/special-orders',{preHandler:staff},async()=>({data:await specialList()}));
app.get('/api/v1/requests/:id/special-orders',{preHandler:auth},async(req,r)=>{const x=(await q('SELECT engineer_id FROM requests WHERE id=$1 AND deleted_at IS NULL',[req.params.id])).rows[0];if(!x)return fail(r,'NOT_FOUND','Заказ не найден',404);if(req.user.role==='ENGINEER'&&Number(x.engineer_id)!==Number(req.user.id))return fail(r,'FORBIDDEN','Нет доступа',403);return{data:await specialList('AND s.request_id=$1',[req.params.id])}});
app.post('/api/v1/requests/:id/special-orders',{preHandler:auth},async(req,r)=>{const b=req.body||{},name=String(b.name||'').trim(),qty=n(b.qty||1);if(!name||qty<=0)return fail(r,'VALIDATION','Укажите запчасть и количество');const order=(await q('SELECT * FROM requests WHERE id=$1 AND deleted_at IS NULL',[req.params.id])).rows[0];if(!order)return fail(r,'NOT_FOUND','Заказ не найден',404);if(req.user.role==='ENGINEER'&&Number(order.engineer_id)!==Number(req.user.id))return fail(r,'FORBIDDEN','Заказ назначен другому инженеру',403);if(['CLOSED','CANCELLED'].includes(order.status))return fail(r,'ORDER_FINISHED','Нельзя заказать запчасть для закрытого заказа',409);const c=await pool.connect();try{await c.query('BEGIN');const part=(await c.query(`INSERT INTO parts(request_id,name,oem_code,qty,purchase_price,sale_price,status,created_by) VALUES($1,$2,$3,$4,0,$5,'REQUESTED',$6) RETURNING *`,[order.id,name,b.oem_code||null,qty,n(b.sale_price),req.user.id])).rows[0];const manager=(await c.query("SELECT id FROM users WHERE role='MANAGER' AND active=true ORDER BY id LIMIT 1")).rows[0]||(await c.query("SELECT id FROM users WHERE role='OWNER' AND active=true ORDER BY id LIMIT 1")).rows[0];const so=(await c.query(`INSERT INTO special_part_orders(request_id,part_id,name,oem_code,qty,sale_price,responsible_id,comment,created_by) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9) RETURNING *`,[order.id,part.id,name,b.oem_code||null,qty,n(b.sale_price),manager?.id||null,b.comment||null,req.user.id])).rows[0];await c.query("UPDATE requests SET status='WAITING_PART',updated_at=now() WHERE id=$1",[order.id]);if(manager?.id)await c.query(`INSERT INTO tasks(title,request_id,assigned_to,priority,due_at,created_by) VALUES($1,$2,$3,'HIGH',now()+interval '2 hours',$4)`,[`Найти и заказать запчасть: ${name}`,order.id,manager.id,req.user.id]);await c.query('INSERT INTO request_history(request_id,user_id,action,details) VALUES($1,$2,$3,$4)',[order.id,req.user.id,'SPECIAL_PART_REQUESTED',{special_order_id:so.id,name,qty,oem_code:b.oem_code||null}]);await c.query('COMMIT');return r.code(201).send({data:so})}catch(e){await c.query('ROLLBACK');throw e}finally{c.release()}});
app.patch('/api/v1/special-orders/:id',{preHandler:staff},async(req,r)=>{const b=req.body||{};const cur=(await q('SELECT * FROM special_part_orders WHERE id=$1',[req.params.id])).rows[0];if(!cur)return fail(r,'NOT_FOUND','Потребность не найдена',404);const status=b.status||cur.status;if(!allowed.has(status))return fail(r,'VALIDATION','Недопустимый статус');const row=(await q(`UPDATE special_part_orders SET supplier=COALESCE($1,supplier),purchase_price=COALESCE($2,purchase_price),sale_price=COALESCE($3,sale_price),expected_at=COALESCE($4,expected_at),responsible_id=COALESCE($5,responsible_id),comment=COALESCE($6,comment),status=$7,updated_at=now() WHERE id=$8 RETURNING *`,[b.supplier??null,b.purchase_price==null?null:n(b.purchase_price),b.sale_price==null?null:n(b.sale_price),b.expected_at||null,b.responsible_id||null,b.comment??null,status,cur.id])).rows[0];if(cur.part_id)await q('UPDATE parts SET purchase_price=$1,sale_price=$2,supplier=$3,status=$4,eta=$5,updated_at=now() WHERE id=$6',[row.purchase_price,row.sale_price,row.supplier,status==='RECEIVED'?'RECEIVED':status==='INSTALLED'?'INSTALLED':'ORDERED',row.expected_at,cur.part_id]);await q('INSERT INTO request_history(request_id,user_id,action,details) VALUES($1,$2,$3,$4)',[cur.request_id,req.user.id,'SPECIAL_PART_STATUS_CHANGED',{special_order_id:cur.id,from:cur.status,to:status,supplier:row.supplier,expected_at:row.expected_at,purchase_price:row.purchase_price}]);return{data:row}});
app.post('/api/v1/special-orders/:id/receive',{preHandler:staff},async(req,r)=>{const c=await pool.connect();try{await c.query('BEGIN');const s=(await c.query('SELECT * FROM special_part_orders WHERE id=$1 FOR UPDATE',[req.params.id])).rows[0];if(!s){await c.query('ROLLBACK');return fail(r,'NOT_FOUND','Потребность не найдена',404)}if(s.status==='RECEIVED'||s.status==='ISSUED'||s.status==='INSTALLED'){await c.query('ROLLBACK');return{data:s}}let wi=null;if(s.oem_code)wi=(await c.query('SELECT * FROM warehouse_items WHERE oem_code=$1 AND active=true LIMIT 1',[s.oem_code])).rows[0];if(!wi)wi=(await c.query('SELECT * FROM warehouse_items WHERE lower(name)=lower($1) AND active=true LIMIT 1',[s.name])).rows[0];if(!wi){const sku=`SP-${Date.now()}-${s.id}`;wi=(await c.query(`INSERT INTO warehouse_items(sku,name,oem_code,supplier,purchase_price,sale_price,quantity,min_quantity,active) VALUES($1,$2,$3,$4,$5,$6,0,0,true) RETURNING *`,[sku,s.name,s.oem_code,s.supplier,s.purchase_price,s.sale_price])).rows[0]}const oldQty=n(wi.quantity),qty=n(s.qty),newQty=oldQty+qty,avg=newQty?((oldQty*n(wi.purchase_price))+(qty*n(s.purchase_price)))/newQty:n(s.purchase_price);await c.query('UPDATE warehouse_items SET quantity=$1,purchase_price=$2,sale_price=GREATEST(sale_price,$3),supplier=COALESCE($4,supplier),updated_at=now() WHERE id=$5',[newQty,avg,s.sale_price,s.supplier,wi.id]);await c.query(`INSERT INTO warehouse_movements(item_id,movement_type,quantity,request_id,unit_cost,sale_price,supplier,comment,created_by) VALUES($1,'RECEIPT',$2,$3,$4,$5,$6,$7,$8)`,[wi.id,qty,s.request_id,s.purchase_price,s.sale_price,s.supplier,`Спецзаказ #${s.id}`,req.user.id]);await setReservation(c,s.request_id,wi.id,qty,req.user.id);await c.query("UPDATE special_part_orders SET status='RECEIVED',warehouse_item_id=$1,received_at=now(),updated_at=now() WHERE id=$2",[wi.id,s.id]);if(s.part_id)await c.query("UPDATE parts SET purchase_price=$1,sale_price=$2,supplier=$3,status='RECEIVED',eta=$4,updated_at=now() WHERE id=$5",[s.purchase_price,s.sale_price,s.supplier,s.expected_at,s.part_id]);const pending=n((await c.query("SELECT count(*) c FROM special_part_orders WHERE request_id=$1 AND id<>$2 AND status NOT IN('RECEIVED','ISSUED','INSTALLED','CANCELLED')",[s.request_id,s.id])).rows[0].c);if(!pending)await c.query("UPDATE requests SET status='REPAIR',updated_at=now() WHERE id=$1 AND status='WAITING_PART'",[s.request_id]);await c.query('INSERT INTO request_history(request_id,user_id,action,details) VALUES($1,$2,$3,$4)',[s.request_id,req.user.id,'SPECIAL_PART_RECEIVED',{special_order_id:s.id,item_id:wi.id,qty}]);const eng=(await c.query('SELECT engineer_id FROM requests WHERE id=$1',[s.request_id])).rows[0];if(eng?.engineer_id)await c.query(`INSERT INTO tasks(title,request_id,assigned_to,priority,due_at,created_by) VALUES($1,$2,$3,'HIGH',now()+interval '4 hours',$4)`,[`Запчасть поступила: ${s.name}. Назначить повторный выезд`,s.request_id,eng.engineer_id,req.user.id]);await c.query('COMMIT');return{data:{...s,status:'RECEIVED',warehouse_item_id:wi.id}}}catch(e){await c.query('ROLLBACK');throw e}finally{c.release()}});
app.post('/api/v1/special-orders/:id/install',{preHandler:auth},async(req,r)=>{const c=await pool.connect();try{await c.query('BEGIN');const s=(await c.query('SELECT s.*,r.engineer_id FROM special_part_orders s JOIN requests r ON r.id=s.request_id WHERE s.id=$1 FOR UPDATE',[req.params.id])).rows[0];if(!s){await c.query('ROLLBACK');return fail(r,'NOT_FOUND','Запчасть не найдена',404)}if(req.user.role==='ENGINEER'&&Number(s.engineer_id)!==Number(req.user.id)){await c.query('ROLLBACK');return fail(r,'FORBIDDEN','Заказ назначен другому инженеру',403)}if(!['RECEIVED','ISSUED'].includes(s.status)){await c.query('ROLLBACK');return fail(r,'STATE_CONFLICT','Запчасть ещё не получена',409)}const wi=(await c.query('SELECT * FROM warehouse_items WHERE id=$1 FOR UPDATE',[s.warehouse_item_id])).rows[0];if(!wi||n(wi.quantity)<n(s.qty)){await c.query('ROLLBACK');return fail(r,'NO_STOCK','Недостаточно остатка',409)}await c.query('UPDATE warehouse_items SET quantity=quantity-$1,updated_at=now() WHERE id=$2',[s.qty,wi.id]);await c.query(`INSERT INTO warehouse_movements(item_id,movement_type,quantity,engineer_id,request_id,unit_cost,sale_price,supplier,comment,created_by) VALUES($1,'INSTALL',$2,$3,$4,$5,$6,$7,$8,$9)`,[wi.id,s.qty,s.engineer_id,s.request_id,s.purchase_price,s.sale_price,s.supplier,`Установка спецзаказа #${s.id}`,req.user.id]);await c.query("UPDATE stock_reservations SET status='RELEASED',released_at=now() WHERE request_id=$1 AND item_id=$2 AND status='ACTIVE'",[s.request_id,wi.id]);await c.query("UPDATE special_part_orders SET status='INSTALLED',installed_at=now(),updated_at=now() WHERE id=$1",[s.id]);if(s.part_id)await c.query("UPDATE parts SET status='INSTALLED',updated_at=now() WHERE id=$1",[s.part_id]);await c.query('INSERT INTO request_history(request_id,user_id,action,details) VALUES($1,$2,$3,$4)',[s.request_id,req.user.id,'SPECIAL_PART_INSTALLED',{special_order_id:s.id,item_id:wi.id,qty:s.qty}]);await c.query('COMMIT');return{data:{ok:true,status:'INSTALLED'}}}catch(e){await c.query('ROLLBACK');throw e}finally{c.release()}});
let busy=false;setInterval(async()=>{if(busy)return;busy=true;try{await resumeReady()}catch(e){app.log.error(e)}finally{busy=false}},30000);app.listen({port:Number(process.env.PORT||8098),host:'0.0.0.0'});